<div id="inventory">
  <div id="backpack">
    <h2>Backpack</h2>
    <div class="inventoryObject" ng-repeat="inv in c.backpack">
      <input ng-model="inv.qty" type="number" />X
      <input ng-model="inv.name" type="text" />
      <button type="button" ng-click="delBackPack($index)" class="btn btn-danger btn-xs">
        <i class="fa fa-times"></i>
      </button>
    </div>
    <input class="addButton" type="button" ng-click="addBackPack()" value="[Add Item]" />
  </div>
  <div id="ammo">
    <h2>Ammo</h2>
    <div class="inventoryObject" ng-repeat="inv in c.ammo">
      <input ng-model="inv.qty" type="number" />X
      <input ng-model="inv.name" type="text" />
      <button type="button" ng-click="delAmmo($index)" class="btn btn-danger btn-xs">
        <i class="fa fa-times"></i>
      </button>
    </div>
    <input class="addButton" type="button" ng-click="addAmmo()" value="[Add Ammo]"/>
  </div>
  <div id="personal">
    <h2>Personal</h2>
    <div class="inventoryObject" ng-repeat="inv in c.personal">
      <input ng-model="inv.qty" type="number" /> X
      <input ng-model="inv.name" type="text" /> @
      <input ng-model="inv.location" type="text" />
      <button type="button" ng-click="delPersonal($index)" class="btn btn-danger btn-xs">
        <i class="fa fa-times"></i>
      </button>
    </div>
    <input class="addButton" type="button" ng-click="addPersonal()" value="[Add Item]" />
  </div>
  <div id="cargo">
    <h2>Cargo</h2>
    <div class="inventoryObject" ng-repeat="inv in c.cargo">
      <input ng-model="inv.qty" type="number" />X
      <input ng-model="inv.name" type="text" />@
      <input ng-model="inv.location" type="text">
      <button type="button" ng-click="delCargo($index)" class="btn btn-danger btn-xs">
        <i class="fa fa-times"></i>
      </button>
    </div>
    <input class="addButton" type="button" ng-click="addCargo()" value="[Add Item]" />
</div>
